spring:
  ##datasource:
    ##url: jdbc:h2:mem:test # 테스트 환경에서는 인메모리 db 지원
    ##username: sa
    ##password:
    ##driver-class-name: org.h2.Driver

  ##jpa:
    ##hibernate:
      ##ddl-auto: create
    ##properties:
      ##hibernate:
        ##show_sql: true # system.out?? ???? ??? ?? ??.
        ##format_sql: true

logging:
  level:
    org.hibernate.sql: debug # logger? ?? ??? ?? ??. ?????? ?? ? ??.
    # org.hibernate.type: trace ?? ? ????? ???? ?? ?? > ??? ?? ?????? ??? ??